---
name: hello-world
description: Placeholder skill for the WordPress Dev (bc-dev-wp) plugin — has no real function yet, and no real skill is planned/in review for it either. Remove this once bc-dev-wp's first real skill lands. Only exists so this plugin has a non-empty skills/ directory, which the marketplace manifest requires to sync correctly.
---

# Hello World (placeholder)

This plugin has no real skills yet. If invoked, just say so plainly and point at the plugin's status in the repo README rather than doing anything else.
