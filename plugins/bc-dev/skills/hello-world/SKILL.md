---
name: hello-world
description: Placeholder skill for the Dev Team (bc-dev) plugin — has no real function yet. Remove this once bc-dev's first real skill lands (git-workflow, PR #8, is the next candidate). Only exists so this plugin has a non-empty skills/ directory, which the marketplace manifest requires to sync correctly.
---

# Hello World (placeholder)

This plugin has no real skills yet. If invoked, just say so plainly and point at the plugin's status in the repo README rather than doing anything else.
